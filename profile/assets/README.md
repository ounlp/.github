# Profile assets

Put images used by `profile/README.md` here.

- **`banner.png`** — a wide hero image (≈ 1280×320). Once added, uncomment the banner
  block at the top of `profile/README.md`. A tornado/Oklahoma-sky motif fits the lab's
  "Through Wind and Rain" identity.
- Optional: `logo.png`, theme icons, a light/dark pair (use an HTML `<picture>` element
  in the README to switch by color scheme).
